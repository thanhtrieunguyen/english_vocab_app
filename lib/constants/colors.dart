import 'package:flutter/material.dart';

class AppColors {
  // Màu chủ đạo từ Hack Não
  static const Color primary = Color(0xFF00BCD4); // Cyan/Teal
  static const Color primaryLight = Color(0xFF4DD0E1);
  static const Color primaryDark = Color(0xFF00838F);
  
  // Màu phụ
  static const Color secondary = Color(0xFF48BB78); // Green
  static const Color accent = Color(0xFFFFA500); // Orange
  
  // Màu nền
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Colors.white;
  
  // Màu text
  static const Color textPrimary = Color(0xFF2D3748);
  static const Color textSecondary = Color(0xFF718096);
  
  // Màu cho memory tips
  static const Color pronunciationColor = Colors.red;
  static const Color meaningColor = Colors.blue;
  
  // Màu trạng thái
  static const Color success = Color(0xFF48BB78);
  static const Color error = Color(0xFFE53E3E);
  static const Color warning = Color(0xFFED8936);
  static const Color info = Color(0xFF3182CE);
}
